<header class="header">
  <nav class="nav">
    <ul class="nav-list">
      <li><a href="#about">ABOUT ME</a></li>
      <li><a href="#experience">EXPERIENCE</a></li>
      <li><a href="#education">EDUCATION</a></li>
      <li><a href="#publications">PUBLICATIONS</a></li>
      <!--<li><a href="#projects">PROJECT</a></li> -->
      <li><a href="#contact">CONTACT</a></li>
    </ul>
  </nav>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: 150px; /* Slightly wider to accommodate horizontal text */
    background-color: #1e1e1e;
    display: flex;
    align-items: center;
    z-index: 10;
  }

  .nav {
    width: 100%;
  }

  .nav-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    align-items: flex-start; /* Changed from center to left-align */
    gap: var(--spacing-lg);
    padding: 0 20px; /* Added horizontal padding */
    margin: 0;
  }

  .nav-list a {
    color: var(--text-secondary);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: var(--transition);
    /* Removed writing-mode and text-orientation */
    padding: 8px 0; /* Adjusted padding */
    display: block; /* Makes entire area clickable */
    width: 100%; /* Full width for hover effect */
  }

  .nav-list a:hover {
    color: var(--accent-color);
  }

  @media (max-width: 768px) {
    .header {
      top: 0;
      left: 0;
      right: 0;
      bottom: auto;
      height: 60px;
      width: 100%;
    }

    .nav-list {
      flex-direction: row;
      gap: var(--spacing-md);
      padding: var(--spacing-sm);
      justify-content: center; /* Center items in mobile */
    }

    .nav-list a {
      padding: 5px 10px;
      /* No need for writing-mode change since we removed it */
    }
  }
</style>